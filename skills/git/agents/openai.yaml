interface:
  display_name: "Git workflow"
  short_description: "Plan branches, PR slices, and agent-safe git"
  brand_color: "#B45309"
  default_prompt: "Use $git to define a branch and PR workflow for this team or redesign."

policy:
  allow_implicit_invocation: true
