interface:
  display_name: "Layout"
  short_description: "Plan skeletons, shells, dimensions, and primitives"
  brand_color: "#111827"
  default_prompt: "Use $layout to plan the layout skeleton and component system before building pages."

policy:
  allow_implicit_invocation: true
