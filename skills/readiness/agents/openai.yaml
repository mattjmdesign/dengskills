interface:
  display_name: "Release readiness"
  short_description: "Assess whether a preview is safe for its intended use"
  brand_color: "#B45309"
  default_prompt: "Use $readiness on this preview and tell me if it is a demo, a pilot, or production."

policy:
  allow_implicit_invocation: true
