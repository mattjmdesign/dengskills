interface:
  display_name: "State coverage"
  short_description: "Find missing loading, error, and edge states"
  brand_color: "#2563EB"
  default_prompt: "Use $states to identify missing gap states for this screen or feature."

policy:
  allow_implicit_invocation: true
