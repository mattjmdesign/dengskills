interface:
  display_name: "UI System Initializer"
  short_description: "Set up tokens, type, spacing, and themes"
  brand_color: "#111827"
  default_prompt: "Use $ui-system-initializer to create a minimal design system foundation before UI work begins."

policy:
  allow_implicit_invocation: true
